type Book {
  title: String
  author: String
}

type Query {
  books: [Book]
}

type Mutation {
  addBook(infos: AddBookInput): [Book]
#   addBook(title: String, author: String): [Book]
}

input AddBookInput {
  title: String!
  author: String!
}
